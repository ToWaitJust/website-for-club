# 社团 AI+IT 业务线网站 — 代码结构文档

> 项目名称：**Infinite Productivity（无限生产力）**
> 仓库路径：`社团AI+IT业务线网站/projects/`
> 部署平台：Coze 云（原生静态模板 `native-static`）
> 技术栈：纯静态 HTML + CSS + JavaScript + Node.js 原生 HTTP 服务器

---

## 一、项目总览

这是一个**纯前端静态站点**，作为"社团 AI+IT 业务线"的门户平台，主要功能：

1. **首页（Landing）**：展示两个核心文档的卡片入口，点击后在 iframe 中预览。
2. **AI+IT 学习规划文档**：从零到一的系统化学习路线，涵盖 13 个阶段、3 张 Mermaid 流程图。
3. **运营方案 V2.3**：社团 & 孵化营的完整运营体系设计，包含积分矩阵、两级考核、12 项风险预案与 22 项待办清单。
4. **Node.js 静态服务器**：提供文件托管和路径重写，用于 Coze 云端部署。

整体架构：**静态 HTML 门户 → Node.js HTTP 服务器 → 浏览器打开**，无需构建步骤、无外部 npm 依赖。

---

## 二、文件结构一览

```
社团AI+IT业务线网站/
└── projects/                          ← 部署根目录（PUBLIC_DIR）
    ├── index.html                     ← 首页门户（Landing + iframe 文档预览）
    ├── ai-learning.html               ← AI+IT 学习规划文档（13 阶段 + 3 张流程图）
    ├── operation.html                 ← 运营方案 V2.3（9 章节 + 4 张流程图）
    ├── server.js                      ← Node.js 原生 HTTP 静态服务器
    ├── styles/
    │   └── main.css                   ← 公共样式（未被实际引用，遗留文件）
    ├── package.json                   ← npm 项目配置（无外部依赖）
    ├── pnpm-lock.yaml                 ← pnpm 锁定文件
    ├── .coze                          ← Coze 云平台部署配置
    └── .gitignore                     ← Git 忽略规则
```

---

## 三、逐文件详细说明

### 1. `index.html` — 首页门户（421 行）

**功能定位**：整个网站的入口页面，提供两个文档的卡片导航和预览。

**页面结构**：

| 区域 | 说明 |
|------|------|
| `#landing`（着陆页） | 居中显示的 Hero 标题 + 两张卡片（"AI+IT 学习规划""运营方案"） |
| `#doc-view`（文档预览） | 点击卡片后切换到的 iframe 容器，顶部有返回按钮和标题徽章 |

**交互逻辑**：

- 点击卡片 → 读取 `data-doc` / `data-title` / `data-badge` 属性，设置 iframe `src`，隐藏着陆页、显示文档预览页。
- 点击"返回"按钮或按 `Escape` 键 → 恢复着陆页，延迟 300ms 后清空 iframe 的 `src` 以释放资源。
- 页面标题随文档切换同步更新。

**技术要点**：

- 纯 CSS 实现，无外部框架，使用 CSS 变量管理主题色。
- 动画：卡片悬停上浮 + 阴影增强，着陆页淡入。
- 响应式：≤680px 时卡片堆叠为竖排，箭头按钮隐藏。

---

### 2. `ai-learning.html` — AI+IT 学习规划 · 正式版（780 行）

**功能定位**：团队 AI+IT 技能学习路线图，从基础技术栈到 AI 工程化的完整规划。

**内容结构（7 个板块）**：

| 板块 | 标题 | 核心内容 |
|------|------|----------|
| 一 | 🔗 学习路径逻辑总览 | 螺旋上升路径说明，最终目标 |
| 二 | 📈 AI 介入程度演变 | **流程图 1**（timeline）：AI 从辅助→接管→进化的三个阶段 |
| 三 | 🛠️ 阶段 1-8（≤200h） | 详细学习表格（含 8 个阶段、每阶段衔接说明 + 推荐资源） |
| 四 | 🗺️ 阶段 1-8 学习路径图 | **流程图 2**（flowchart TD）：技术栈依赖关系 |
| 五 | 🤖 阶段 9-13（不限时） | AI 工程化进阶：DSPy、LangGraph、CrewAI、Harness、AgentEvolver/SCOPE |
| 六 | 🧩 完整学习路径总图 | **流程图 3**（flowchart LR）：技术栈→AI 工程化的全貌，含硬性门槛 Gate |
| 七 | 📊 总学时对比 | 三个阶段学时汇总表 |

**技术要点**：

- 引入 Mermaid 10.9.0（CDN）渲染 3 张流程图。
- 每张图表带缩放控制（`+ / - / 重置` 按钮），支持 30%~300% 缩放。
- 自定义 Mermaid 主题：浅蓝节点背景 + 深色文字，适合文档阅读。
- 响应式：≤700px 时缩小间距和字号。

**阶段 1-8 学时分配**：

| 阶段 | 内容 | 学时 |
|------|------|------|
| 1 | Java 核心速通 | 12h |
| 2 | 数据库 + Maven | 12h |
| 3 | Spring Boot + 芋道后端架构 | 40h |
| 4 | Redis 快速上手 | 5h |
| 5 | 前端基础 + 芋道前端架构 | 35h |
| 6 | UniApp 跨端开发 | 12h |
| 7 | 部署与测试 | 25h |
| 8 | 全栈整合与复盘 | 15h |
| **合计** | | **~156h（≤200h）** |

---

### 3. `operation.html` — 运营方案 V2.3（1524 行）

**功能定位**：AI+IT 社团 & 孵化营的完整运营方案，四大部分九章节，内部使用文档。

**内容结构（4 部分 9 章节）**：

| 部分 | 章节 | 核心内容 |
|------|------|----------|
| **PART 1 · 顶层设计** | 01 核心逻辑与整体架构 | 漏斗式人才培养体系、两条核心原则、两级考核、学员成长进阶路径（流程图 1+2） |
| **PART 2 · 学员成长主线** | 02 积分激励机制 | 100 分制、积分计算三步法、模块矩阵表（7 模块） |
| | 03 积分判断机制 | 费曼学习法 + AI 精细化打分 |
| | 04 过程性材料收集 | 文字反思（飞书）+ 实操代码（私域仓库）双轨管理 |
| | 05 考核体系与资源输送 | 两级考核门槛（80分/90分）、补考流转、资源对接、成长记录册 |
| **PART 3 · 运营保障体系** | 06 运营人员职责 | 五大职责、激励制度（物质+发展）、反馈话术模板 |
| | 07 运营工具与表格设计 | 工具选型（电子表格→多维表格）、11 个核心字段表、自动工作流 |
| **PART 4 · 风险管控** | 08 问题预案 | 12 项风险点总览（高6/中4/低2）、每风险含触发场景+应急处理、L1-L3 三级升级机制（流程图 3+4） |
| | 09 待办事项 | 22 项待办（紧急 6 项 / 短期 8 项 / 长期 8 项） |

**技术要点**：

- 引入 Google Fonts（Work Sans / Outfit / DM Mono / IBM Plex Serif）。
- Mermaid 10（CDN）渲染 4 张流程图，每张图表带缩放 + 拖拽 + 鼠标滚轮缩放功能。
- 自定义风险卡片样式：按风险等级（高/中/低）显示不同颜色左边框。
- 表格支持 sticky 表头，最大高度 600px 时自动滚动。
- 目录（TOC）分四部分，双列网格布局。
- 响应式：≤768px 时目录单列、风险卡片上下堆叠。

---

### 4. `server.js` — Node.js 静态文件服务器（73 行，已修复路径 bug）

**功能定位**：Coze 云端部署的 HTTP 服务器，提供静态文件服务。

**核心逻辑**：

```
请求 → URL 解析 → 路径重写(REWRITES) → 读取文件 → 按 MIME 类型流式返回
```

| 功能 | 说明 |
|------|------|
| **端口** | `DEPLOY_RUN_PORT` 环境变量，默认 5000 |
| **路径重写** | `/ai-learning` → `ai-learning.html`，`/operation` → `operation.html` |
| **404 处理** | 静态站点，文件不存在直接返回 404（无 SPA 回退） |
| **MIME 类型** | 支持 HTML/CSS/JS/JSON/图片/字体/PDF 共 12 种类型 |
| **流式输出** | 文本文件以 UTF-8 编码流式返回，二进制文件直接流式 |

**启动方式**：`node server.js`（见 `package.json` 的 `dev` / `start` 脚本）

---

#### ⚠️ Bug 修复记录：POSIX 路径拼接问题（2026-08-18）

**症状**：部署到 Coze 云平台后，访问 `/ai-learning` 和 `/operation` 返回 404，页面显示服务器启动日志。

**根因**：原代码中 `path.join` 在 POSIX（Linux）上遇到第二个参数以 `/` 开头时，会丢弃前面的目录路径，直接返回绝对路径：

```javascript
// ❌ 修复前（Linux 上的实际行为）
path.join('/app', '/ai-learning.html')  →  '/ai-learning.html'  ← 文件系统根目录，找不到文件！

// ✅ 修复后
path.join('/app', 'ai-learning.html')   →  '/app/ai-learning.html'  ← 正确路径
```

**修改内容（3 处）**：

| 修改点 | 修复前 | 修复后 |
|--------|--------|--------|
| REWRITES 值 | `'/ai-learning.html'`（带前导 `/`） | `'ai-learning.html'`（相对路径） |
| 根路径默认值 | `'/index.html'` | `'index.html'` |
| 文件路径解析 | `path.join(PUBLIC_DIR, pathname)` | `path.join(PUBLIC_DIR, pathname.replace(/^\//, ''))` |
| 文件不存在处理 | SPA fallback（返回 index.html） | 直接返回 404 |

**为什么 `/` 不受影响**：根路径 `'/'` 被显式改为 `'index.html'`（相对路径），`path.join` 能正确拼接。

---

### 5. `styles/main.css` — 公共样式（136 行）

**功能定位**：原生静态模板自带的默认样式文件。

**内容**：提供基础 Reset、主题变量（亮/暗色）、App 容器居中布局、响应式适配。

**状态**：当前项目的 HTML 文件均使用**内联 `<style>`**，未实际引用此 CSS 文件。属于模板遗留文件，可保留或删除。

---

### 6. `package.json` — npm 项目配置（11 行）

| 字段 | 值 | 说明 |
|------|------|------|
| `name` | `infinite-productivity` | 项目名称 |
| `version` | `1.0.0` | 版本号 |
| `private` | `true` | 不发布到 npm |
| `scripts.dev` | `node server.js` | 开发启动 |
| `scripts.start` | `node server.js` | 生产启动 |
| `scripts.build` | `""` | 空（无构建步骤） |
| `dependencies` | `{}` | **零外部依赖** |

---

### 7. `.coze` — Coze 云平台部署配置（23 行）

**功能定位**：告知 Coze 平台如何构建和运行此项目。

| 配置项 | 值 | 说明 |
|--------|------|------|
| `entrypoint` | `index.html` | 应用入口文件 |
| `requires` | `["nodejs-24"]` | 运行时要求 Node.js 24 |
| `project_type` | `web` | 项目类型 |
| `template` | `native-static` | 原生静态模板 |
| `version` | `0.0.35` | 模板版本 |
| `dev.run` | `node server.js` | 开发环境启动命令 |
| `deploy.run` | `node server.js` | 部署环境启动命令 |
| `dev/build` | `pnpm install` | 构建步骤（本项目无依赖，实际为空操作） |

---

### 8. `.gitignore` — Git 忽略规则（95 行）

**功能定位**：Git 版本控制忽略规则。

**忽略内容分类**：

- **构建产物**：`node_modules`、`dist`、`.next`、`.nuxt`、`.turbo`、`.vercel` 等
- **包管理器**：`.pnp*`、`.npm`
- **Python 工具**：`__pycache__/`、`*.pyc`、`.venv/*`、`.pytest_cache/` 等
- **Go 工具**：`bin/`、`*.test`
- **编辑器**：`.vscode/`、`.idea/`、`*.iml`
- **操作系统**：`.DS_Store`、`Thumbs.db`
- **日志**：`*.log`、`output.log`
- **媒体文件**：视频（mp4/avi/mov等）、归档（iso/dmg/rar/zip/gz）、文档（pdf/docx/xlsx/pptx/csv）
- **Coze 平台**：`.cozeproj/reduction`、`.preview`、`.codegraph`

---

### 9. `pnpm-lock.yaml` — pnpm 锁定文件

**功能定位**：记录 pnpm 包管理器的依赖锁定信息。

**状态**：由于 `package.json` 中 `dependencies` 为空，此文件仅包含空锁定记录，实际无作用。属于 pnpm 初始化时自动生成的默认文件。

---

## 四、页面交互流程图

```
用户访问网站
     │
     ▼
┌─────────────────────┐
│  index.html 着陆页  │  ← 显示 Hero 标题 + 两张卡片
└──────────┬──────────┘
           │ 点击卡片
           ▼
┌──────────────────────────────┐
│  #doc-view 文档预览模式      │  ← 顶部返回按钮 + iframe 加载文档
│                              │
│  iframe src = ai-learning    │────→ 学习规划文档
│       或 operation.html      │────→ 运营方案文档
└──────────┬───────────────────┘
           │ 点击返回 / 按 Escape
           ▼
┌─────────────────────┐
│  回到着陆页          │  ← 延迟 300ms 清空 iframe
└─────────────────────┘
```

---

## 五、部署架构

```
┌──────────────────────────────────────────┐
│           Coze 云平台                     │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  Node.js 24 运行时                  │  │
│  │                                    │  │
│  │  server.js                         │  │
│  │  ├── 监听端口 (DEPLOY_RUN_PORT)     │  │
│  │  ├── 路径重写 (/ai-learning → .html)│  │
│  │  └── 静态文件流式输出              │  │
│  │                                    │  │
│  │  文件:                             │  │
│  │  ├── index.html     ← 首页门户     │  │
│  │  ├── ai-learning.html ← 学习规划    │  │
│  │  ├── operation.html   ← 运营方案    │  │
│  │  └── styles/main.css ← (未使用)     │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
         │ HTTPS
         ▼
    浏览器（用户）
```

---

## 六、关键设计特征汇总

| 维度 | 说明 |
|------|------|
| **零依赖** | 除 CDN 上的 Mermaid 外，无任何 npm 包依赖 |
| **纯静态** | 所有页面为纯 HTML，无后端 API 调用 |
| **单文件服务器** | 83 行 `server.js`，原生 Node.js 实现 |
| **响应式** | 三个 HTML 文件均包含移动端适配（breakpoint：680px / 700px / 768px） |
| **图表交互** | 支持缩放、拖拽、鼠标滚轮、触屏双指缩放 |
| **无构建** | `build` 脚本为空，`pnpm install` 后直接 `node server.js` |
| **Coze 原生** | `.coze` 配置文件声明为 `native-static` 模板 |
